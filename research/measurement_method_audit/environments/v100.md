# Environment V100 — Detail (/home/yaoyunchao/conteb-v100-20260826)

> Source files probed 2026-08-30: `F:\papers\reproduction\v100\author-v100-training-config.yml`, `author-v100-memory-probe-config.yml`, `author-v100-smoke-config.yml`, `bootstrap-v100-env.sh`, `remote-init.sh`, `run-author-accelerate.sh`, `README.md`. No other V100 doc found in `F:\AIinfraResearch`. No guessing.

## Declared vs UNKNOWN

- **Declared**: 2 GPUs required (`n_gpus: 2`, `assert device_count >=2`), 32 GiB per V100 (comment), Volta SDPA path (flash unavailable), torch 2.2.2+cu121, Python 3.10, transformers 4.48.0, datasets 3.2.0, accelerate 1.7.0, pylate 1.0.0, path `/home/yaoyunchao/conteb-v100-20260826`, DDP `accelerate launch --multi_gpu --num_processes 2 --main_process_port 29610`, ModernBERT 4096 tokens per_device 2 → global 4, gradient_checkpointing reentrant=false, fp16 autocast.
- **UNKNOWN / NOT DOCUMENTED**: PCIe/SXM model, NVLink/PCIe bandwidth, CPU model/cores, RAM total, Linux distro + version, kernel version, NVIDIA driver version (only "CUDA 12.2 support"), CUDA runtime full version, NCCL version/backend detail, network/storage speeds, GPU topology (which slots, NVLink mesh), power/clocks policy, scheduler, other users contention, concurrent allocations, SSH host alias, VPN, campus-net requirement, tunnel, firewall, current reachability, external IP. Every UNKNOWN so marked per task §3.1.

## Software stack (V100 training domain)
```
torch==2.2.2+cu121
transformers==4.48.0
accelerate==1.7.0
sentence-transformers==3.3.1
datasets==3.2.0
configue==5.0.0
pylate==1.0.0
Python 3.10
CUDA driver supports 12.2
```

## Serving relevance
This V100 env is configured for `contextual-embeddings` training (ModernBERT long-context), not for `research/measurement/harness/hf_server.py` serving. No `hf_server.py` hash, no `vllm`/`sglang`, no TTFT harness validated on it. Using it for serving L0 requires installing `hf_transformers` stack (`torch`, `transformers 5.16.1`, `hf_server.py` 39cc7cd90bac) or vllm 0.28+ in a fresh venv, with `tiny-gpt2` or quantized model fitting 32G (trivial). No change to global driver/kernel without operator approval; only user-space port 8036 isolated.

## Access status: BLOCKED
- Exhaustive grep found no `~/.ssh/config` Host, no deployment script with SSH destination, no cluster doc with IP.
- `research/environment_linux.md` §1: "Native Linux GPU server BLOCKED — no IP, no SSH, no second host".
- House rule: failed probe leaves availability unconfirmed; we report BLOCKED, not NOT EXIST. Requires operator to supply host alias / VPN / tunnel.

## Single vs Dual GPU policy (§9)
Start single-V100 (TP=1, `CUDA_VISIBLE_DEVICES=0`) for measurement validation to isolate serving variance from TP/NCCL/NVLink. Log `nvidia-smi topo -m` and `NCCL_DEBUG` if dual later. Dual only after single passes L0.

## Limitations if used
- Model must be `sshleifer/tiny-gpt2` 124M for comparability with Windows, or note quantization difference if using larger model (V100 32G can host 7B FP16 vs 4060 8G cannot → absolute ms not comparable; stationarity only).
- Must re-probe mutable facts before claiming AVAILABLE: `nvidia-smi`, `torch.cuda.is_available`, `uname -a`, `cat /etc/os-release` stored byte-faithfully.
- Must not interfere with other users, kill foreign PIDs, or modify global CUDA/driver.

## Exact reproduction steps (once access granted)
```
ssh <host> -- nvidia-smi --query-gpu=index,name,memory.total,driver_version --format=csv
ssh <host> -- python3 -c "import torch, platform; print(torch.__version__, torch.cuda.is_available(), torch.cuda.device_count()); print(platform.platform())"
ssh <host> -- uname -a; cat /etc/os-release; nvidia-smi -q | head -n 80
# then single-V100 L0:
# on V100: CUDA_VISIBLE_DEVICES=0 python -m research.measurement.harness.run_stage3e --model sshleifer/tiny-gpt2 --c 1,4 --reps 5 --n 40 --workload 512/64 --client pooled --L0 OFF --port 8036
```

*Do not expand scope to multi-GPU until single passes.*
